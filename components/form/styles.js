import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  formTitle: {
    fontSize: 20,
    textAlign: "center",
    marginTop: 20,
  },
  formWrapper: {
    marginTop: 20,
  },
  formInput: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
});
